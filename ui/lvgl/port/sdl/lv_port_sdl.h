#ifndef LV_PORT_SDL_H
#define LV_PORT_SDL_H

/* SDL display and input is handled by LVGL's built-in SDL driver.
 * Configuration is in lv_conf.h (LV_USE_SDL = 1).
 * No additional port code needed for desktop builds. */

#endif /* LV_PORT_SDL_H */
